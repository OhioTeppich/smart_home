/// Maximum number of devices the user can add to the dashboard's
/// Schnellzugriff widget. Chosen so the tiles fit exactly into the 2x2 grid
/// `QuickAccessCard` renders (see `quick_access_card.dart`).
const kQuickAccessMaxDevices = 4;
